spring:
  jpa:
    properties:
      hibernate.default_schema: warehouse
  datasource:
    url: jdbc:postgresql://localhost:5432/warehouse
    username: postgres
    password: postgres

app:
  notifications:
    email:
      enabled: true
      name: "email"
    sms:
      enabled: true
      name: "sms"
